node_modules/
dist/
*.log
.DS_Store
.env
# scratch dir: local SQLite mirror, generated images, scan/score JSON dumps
tmp/
/thoughts/

# Claude Code local state (scheduler locks, etc.) — never commit
.claude/
