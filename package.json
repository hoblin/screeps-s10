{
  "name": "screeps-s10",
  "version": "0.1.0",
  "description": "Hoblin's Screeps AI — Season 10. OOP architecture inspired by Overmind, written in clean modern JS.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:main": "node build.mjs --main",
    "watch": "node build.mjs --watch",
    "deploy": "node deploy.mjs",
    "deploy:main": "npm run build:main && node deploy.mjs",
    "deploy:season": "npm run build && node deploy.mjs --server https://screeps.com/season",
    "lint": "echo \"(no linter yet)\""
  },
  "author": "hoblin",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.21.5",
    "screeps-api": "^1.16.1"
  }
}
